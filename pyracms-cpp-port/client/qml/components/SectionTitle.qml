import QtQuick
import QtQuick.Controls
import QtQuick.Layouts
import Hypernucleus

// Bold heading used between sections of the game page.
Label {
    color: Theme.textBright
    font.pixelSize: Theme.fontLarge
    font.bold: true
    Layout.topMargin: Theme.spaceM
}
